@* View criada a partir do metodo add do client controller (Seleciona palavra View que retorna do metodo e click dir -> addview) *@

@model ServiceOrderManagerV3.Models.AddClientViewModel
<h1>Add Client</h1>



<form method="post">

    <div class="mt-3">
        <label class="form-label">Name</label>
        <input type="text" class="form-control" asp-for="Name" />
    </div>

    <div class="mt-3">
        <label class="form-label">Email</label>
        <input type="email" class="form-control" asp-for="Email" />
    </div>

    <div class="mt-3">
        <label class="form-label">CNPJ</label>
        <input type="text" class="form-control" asp-for="Cnpj" />
    </div>

    <div class="mt-3">
        <label class="form-label">Address</label>
        <input type="text" class="form-control" asp-for="Address" />
    </div>

    @* Add button  *@

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Save</button>
    </div>
</form>